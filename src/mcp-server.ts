#!/usr/bin/env node
/**
 * MCP server entry point — exposes dynamic analysis tools to LLMs.
 * Tools are grouped into: observation, action, introspection, verdict.
 */
import { logger } from './logger.js';

// TODO: Implement MCP server using @modelcontextprotocol/sdk
// This will expose the tools defined in src/mcp/tools/

logger.info('MCP server scaffold — implementation pending');
